function conversionСurrency1 (moneyInRub){ 
   const dollaToRub = 92.41 ; // курс доллара к рублю
    const rubIndollar = moneyInRub / dollaToRub

 return rubIndollar
} 
const firstOperation = conversionСurrency1(1000) ; // первый круг прокрутки валюты 
console.log(firstOperation) // в долларах 

function conversionСurrencySecond (moneyInUSD) {
    const dollarToEur = 0.91 ; // курс доллара к евро 
   const dollarInEuro = moneyInUSD / dollarToEur
   return dollarInEuro
}
const secondOperation = conversionСurrencySecond(firstOperation)
console.log(secondOperation) // в евро 


//НЕ ПОНЯЛ КАК СДЕЛАТЬ,ЧТОБЫ ВЫВОДИЛО NULL
